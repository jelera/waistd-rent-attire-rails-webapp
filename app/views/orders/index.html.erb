
<h2>All Orders</h2>

<% @orders.each do |order| %>
  <p>Name: <%= order.name %></p>
  <p>Email: <%= order.email %></p>
  <p>Line Items: </p>
  <ul>
    <% order.cart_products.each do |product| %>
    <li><%= product.quantity %> x <%= product.product.name %></li>
    <% end %>
  </ul>
<% end %>
